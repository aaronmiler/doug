{
  "name": "doug",
  "private": true,
  "description": "An opinionated personal coding assistant — a branded pi distribution with profile onboarding and human-task guardrails",
  "scripts": {
    "postinstall": "patch-package",
    "test": "for f in test/*.test.ts; do node --experimental-strip-types --no-warnings \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.10"
  },
  "devDependencies": {
    "patch-package": "^8.0.1"
  }
}
